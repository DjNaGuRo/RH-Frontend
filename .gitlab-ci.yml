image: node:12.19.0

stages:
  - build
  - deploy
  - others

cache:
  paths:
    - node_modules/

before_script:
  - npm install

compile:
  stage: build
  script:
    - npm run build
  except:
    - main

npm_scripts:
  stage: others
  variables:
    PROJECT_ROOT: "."
    NPM_SCRIPTS_OPTION: "lint"
    NPM_SCRIPTS: "ng;build"
    NPM_OUTPUT: "public"
  script:
    - cd $PROJECT_ROOT
    - export IFS=";"
    - for script in $NPM_SCRIPTS; do
    -   npm run $script $NPM_SCRIPTS_OPTION
    - done
    - mkdir $NPM_OUTPUT
    - cp -R dist/ui/* public
  artifacts:
    paths:
      - $NPM_OUTPUT

# pages:
#   stage: deploy
#   script:
#     - npm run build
#     - mkdir public
#     - cp -R dist/ui/* public
#   artifacts:
#     paths:
#       - public
#   only:
#     - main
